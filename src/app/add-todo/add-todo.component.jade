doctype html
form((ngSubmit)="add();")
   input(
      class="add-todo" 
      placeholder="What needs to be done?" 
      autofocus 
      required
      [(ngModule)]="newTodoContent",
      (keyup)="validateContent(newTodoContent)"
   )  